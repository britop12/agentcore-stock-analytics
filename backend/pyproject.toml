[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "aws-stock-agent"
version = "0.1.0"
description = "Streaming AI agent API on AWS Agentcore Runtime"
requires-python = ">=3.11"
